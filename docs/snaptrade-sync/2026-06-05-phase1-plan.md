# snaptrade-sync Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用用户自己的免费 SnapTrade key 把嘉信(Schwab)持仓(股票+期权+现金)写进自托管 wealthfolio，复刻 ibkr-sync 的快照模式；Phase 1 不含已实现盈亏。

**Architecture:** 新工具 `tools/snaptrade-sync`。`sync.ts` 用 SnapTrade 官方 TS SDK 直接拉 `getUserHoldings`，纯函数 `mapping.ts` 转成 wealthfolio `HoldingsPositionInput` + 期权资产规格，经复用自 ibkr-sync 的 `wealthfolio.ts` HTTP 客户端 POST `/snapshots/import` 与 `/market-data/quotes/import`。无 `claude -p`/MCP 层。

**Tech Stack:** TypeScript + tsx(运行) + vitest(测试) + zod(校验) + snaptrade-typescript-sdk。Node 22+。

参见 spec：`docs/snaptrade-sync/2026-06-05-design.md`。复用蓝本：`tools/ibkr-sync/`。

---

## 文件结构

```
tools/snaptrade-sync/
├── package.json              # 新建
├── tsconfig.json             # 复制自 ibkr-sync
├── src/
│   ├── snaptrade.ts          # 新建: SDK 封装 + zod schema + fetch
│   ├── mapping.ts            # 新建: 纯函数映射(本 Phase 不含 realized)
│   ├── state.ts              # 复制自 ibkr-sync + 适配 ObservedPosition 构造
│   ├── wealthfolio.ts        # 复制自 ibkr-sync, 原样
│   └── sync.ts               # 新建: 编排(holdings only)
├── test/
│   ├── fixtures/holdings.json # 新建: 本次实测真实持仓(脱敏)
│   ├── snaptrade.test.ts     # schema 解析
│   ├── mapping.test.ts       # 映射纯函数
│   └── state.test.ts         # 复制自 ibkr-sync 关键用例
├── scripts/
│   ├── run-snaptrade.sh      # 新建: source 秘钥 + npx tsx(无 claude -p)
│   └── install-cron.sh       # 复制自 ibkr-sync, 改 tag
└── state/                    # 运行时生成, gitignored
docs/snaptrade-sync/
└── secrets.local.sh          # gitignored: export SNAPTRADE_*/WEALTHFOLIO_*
```

复用矩阵：`wealthfolio.ts` 100% 原样；`state.ts` ~95%(只改 observed 构造与 version)；`mapping.ts`/`snaptrade.ts`/`sync.ts` 新写。

---

### Task 0: 脚手架 + 复用文件

**Files:**
- Create: `tools/snaptrade-sync/package.json`
- Create: `tools/snaptrade-sync/tsconfig.json` (copy)
- Create: `tools/snaptrade-sync/src/wealthfolio.ts` (copy)
- Create: `tools/snaptrade-sync/.gitignore`

- [ ] **Step 1: 建目录与 package.json**

`tools/snaptrade-sync/package.json`:
```json
{
  "name": "@wealthfolio-tools/snaptrade-sync",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "sync": "tsx src/sync.ts"
  },
  "dependencies": {
    "snaptrade-typescript-sdk": "^9",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: 复制 tsconfig 与 wealthfolio.ts、写 .gitignore**

```bash
cd tools/snaptrade-sync
cp ../ibkr-sync/tsconfig.json ./tsconfig.json
cp ../ibkr-sync/src/wealthfolio.ts ./src/wealthfolio.ts
printf 'node_modules/\nstate/\nlogs/\n' > .gitignore
```
`wealthfolio.ts` 与券商无关（agent 已确认 100% 可复用），原样复制。其导出类型 `HoldingsPositionInput`、`HoldingsSnapshotInput`、`NewAssetInput`、`OptionSpec`、`AccountInput`、`QuoteImport`、`WealthfolioClient` 后续直接 import。

- [ ] **Step 3: 安装依赖（装包走有线代理）**

Run:
```bash
cd tools/snaptrade-sync && http_proxy=http://109.105.230.22:9090 https_proxy=http://109.105.230.22:9090 npm install --no-audit --no-fund
```
Expected: `added N packages`，生成 node_modules + package-lock.json。

- [ ] **Step 4: typecheck 基线**

Run: `cd tools/snaptrade-sync && npx tsc --noEmit`
Expected: PASS（wealthfolio.ts 自洽，无报错）。

- [ ] **Step 5: Commit**

```bash
git add tools/snaptrade-sync/package.json tools/snaptrade-sync/tsconfig.json tools/snaptrade-sync/src/wealthfolio.ts tools/snaptrade-sync/.gitignore tools/snaptrade-sync/package-lock.json
git commit -m "chore(snaptrade-sync): scaffold + reuse wealthfolio.ts from ibkr-sync"
```

---

### Task 1: snaptrade.ts — SDK 封装 + zod schema + fetch

**Files:**
- Create: `tools/snaptrade-sync/test/fixtures/holdings.json`
- Create: `tools/snaptrade-sync/test/snaptrade.test.ts`
- Create: `tools/snaptrade-sync/src/snaptrade.ts`

- [ ] **Step 1: 落 fixture（用真实形状，脱敏）**

`test/fixtures/holdings.json`（截取本次实测的真实结构，留 1 股票 + 1 多头期权 + 1 空头期权 + balances）:
```json
{
  "positions": [
    { "symbol": { "symbol": "TSLA", "exchange": { "mic_code": "XNAS" } }, "price": 420, "units": 100, "currency": null, "average_purchase_price": 250.1 }
  ],
  "option_positions": [
    { "symbol": { "option_symbol": { "ticker": "MSFT  270115C00500000", "strike_price": 500, "expiration_date": "2027-01-15", "is_mini_option": false, "option_type": "CALL", "underlying_symbol": { "symbol": "MSFT" } } }, "price": 22.975, "units": 1, "currency": null, "average_purchase_price": 30.0066 },
    { "symbol": { "option_symbol": { "ticker": "HSAI  260618P00020000", "strike_price": 20, "expiration_date": "2026-06-18", "is_mini_option": false, "option_type": "PUT", "underlying_symbol": { "symbol": "HSAI" } } }, "price": 0.875, "units": -2, "currency": null, "average_purchase_price": 0.99335 }
  ],
  "balances": [
    { "currency": { "code": "USD" }, "cash": 46575.75, "buying_power": 46575.75 }
  ]
}
```

- [ ] **Step 2: 写解析失败测试**

`test/snaptrade.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { parseHoldings } from "../src/snaptrade.js";

const raw = JSON.parse(readFileSync(new URL("./fixtures/holdings.json", import.meta.url), "utf8"));

describe("parseHoldings", () => {
  it("parses positions, option_positions, balances", () => {
    const h = parseHoldings(raw);
    expect(h.positions).toHaveLength(1);
    expect(h.option_positions).toHaveLength(2);
    expect(h.balances[0].currency.code).toBe("USD");
  });
  it("keeps option metadata and signed units", () => {
    const h = parseHoldings(raw);
    const short = h.option_positions.find(o => o.symbol.option_symbol.ticker.startsWith("HSAI"))!;
    expect(short.units).toBe(-2);
    expect(short.symbol.option_symbol.option_type).toBe("PUT");
    expect(short.symbol.option_symbol.is_mini_option).toBe(false);
  });
  it("tolerates missing optional arrays", () => {
    expect(parseHoldings({ positions: [] }).option_positions).toEqual([]);
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd tools/snaptrade-sync && npx vitest run test/snaptrade.test.ts`
Expected: FAIL（`parseHoldings` 未定义 / 模块不存在）。

- [ ] **Step 4: 实现 snaptrade.ts**

`src/snaptrade.ts`:
```ts
import { Snaptrade } from "snaptrade-typescript-sdk";
import { z } from "zod";

const underlyingSchema = z.object({ symbol: z.string() }).passthrough();
const optionSymbolSchema = z.object({
  ticker: z.string(),
  strike_price: z.number(),
  expiration_date: z.string(),
  is_mini_option: z.boolean().nullish(),
  option_type: z.enum(["CALL", "PUT"]),
  underlying_symbol: underlyingSchema,
}).passthrough();

export const optionPositionSchema = z.object({
  symbol: z.object({ option_symbol: optionSymbolSchema }).passthrough(),
  price: z.number().nullable(),
  units: z.number(),
  currency: z.any().nullish(),
  average_purchase_price: z.number().nullable(),
}).passthrough();

export const equityPositionSchema = z.object({
  symbol: z.object({
    symbol: z.string(),
    exchange: z.object({ mic_code: z.string().nullish() }).passthrough().nullish(),
  }).passthrough(),
  price: z.number().nullable(),
  units: z.number(),
  currency: z.any().nullish(),
  average_purchase_price: z.number().nullable(),
}).passthrough();

export const balanceSchema = z.object({
  currency: z.object({ code: z.string() }).passthrough(),
  cash: z.number(),
}).passthrough();

export const holdingsSchema = z.object({
  positions: z.array(equityPositionSchema).default([]),
  option_positions: z.array(optionPositionSchema).default([]),
  balances: z.array(balanceSchema).default([]),
}).passthrough();

export type Holdings = z.infer<typeof holdingsSchema>;
export type OptionPosition = z.infer<typeof optionPositionSchema>;
export type EquityPosition = z.infer<typeof equityPositionSchema>;

export function parseHoldings(raw: unknown): Holdings {
  return holdingsSchema.parse(raw);
}

export function makeClient(clientId: string, consumerKey: string): Snaptrade {
  return new Snaptrade({ clientId, consumerKey });
}

export async function listAccounts(client: Snaptrade, userId: string, userSecret: string) {
  const { data } = await client.accountInformation.listUserAccounts({ userId, userSecret });
  return data as Array<{ id: string; name: string; institution_name: string }>;
}

export async function fetchHoldings(
  client: Snaptrade, userId: string, userSecret: string, accountId: string,
): Promise<Holdings> {
  const { data } = await client.accountInformation.getUserHoldings({ userId, userSecret, accountId });
  return parseHoldings(data);
}
```

- [ ] **Step 5: 运行确认通过**

Run: `cd tools/snaptrade-sync && npx vitest run test/snaptrade.test.ts`
Expected: PASS（3 个用例全过）。

- [ ] **Step 6: Commit**

```bash
git add tools/snaptrade-sync/src/snaptrade.ts tools/snaptrade-sync/test/snaptrade.test.ts tools/snaptrade-sync/test/fixtures/holdings.json
git commit -m "feat(snaptrade-sync): SnapTrade SDK wrapper + zod holdings parsing"
```

---

### Task 2: mapping.ts — 持仓/现金 → wealthfolio 输入

**Files:**
- Create: `tools/snaptrade-sync/src/mapping.ts`
- Create: `tools/snaptrade-sync/test/mapping.test.ts`

- [ ] **Step 1: 写映射测试**

`test/mapping.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { parseHoldings } from "../src/snaptrade.js";
import {
  optionPositionToHoldingsPosition, buildOptionAssetSpec,
  equityPositionToHoldingsPosition, balancesToCashBalances, optionMultiplier,
} from "../src/mapping.js";

const h = parseHoldings(JSON.parse(readFileSync(new URL("./fixtures/holdings.json", import.meta.url), "utf8")));
const call = h.option_positions.find(o => o.symbol.option_symbol.ticker.startsWith("MSFT"))!;
const shortPut = h.option_positions.find(o => o.symbol.option_symbol.ticker.startsWith("HSAI"))!;
const eq = h.positions[0];

describe("optionMultiplier", () => {
  it("100 for standard, 10 for mini", () => {
    expect(optionMultiplier(call)).toBe(100);
    expect(optionMultiplier({ ...call, symbol: { option_symbol: { ...call.symbol.option_symbol, is_mini_option: true } } } as any)).toBe(10);
  });
});

describe("optionPositionToHoldingsPosition", () => {
  it("maps OCC/qty/avgCost, signed short", () => {
    const row = optionPositionToHoldingsPosition(shortPut, "asset-uuid-1");
    expect(row).toMatchObject({
      assetId: "asset-uuid-1", symbol: "HSAI  260618P00020000",
      quantity: "-2", avgCost: "0.99335", currency: "USD", instrumentType: "OPTION",
    });
  });
});

describe("buildOptionAssetSpec", () => {
  it("builds NewAsset with option metadata + multiplier", () => {
    const spec = buildOptionAssetSpec(call);
    expect(spec).toMatchObject({
      quoteMode: "MANUAL", quoteCcy: "USD", instrumentType: "OPTION",
      instrumentSymbol: "MSFT  270115C00500000", displayCode: "MSFT  270115C00500000",
    });
    expect(spec.metadata!.option).toMatchObject({
      underlyingAssetId: "MSFT", expiration: "2027-01-15", right: "CALL",
      strike: 500, multiplier: "100", occSymbol: "MSFT  270115C00500000",
    });
  });
});

describe("equityPositionToHoldingsPosition", () => {
  it("maps symbol/qty/avgCost/mic", () => {
    expect(equityPositionToHoldingsPosition(eq)).toMatchObject({
      symbol: "TSLA", quantity: "100", avgCost: "250.1", currency: "USD",
      instrumentType: "EQUITY", exchangeMic: "XNAS",
    });
  });
});

describe("balancesToCashBalances", () => {
  it("maps per-currency cash", () => {
    expect(balancesToCashBalances(h.balances)).toEqual({ USD: "46575.75" });
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `cd tools/snaptrade-sync && npx vitest run test/mapping.test.ts`
Expected: FAIL（`mapping.js` 不存在）。

- [ ] **Step 3: 实现 mapping.ts**

`src/mapping.ts`:
```ts
import type { HoldingsPositionInput, NewAssetInput } from "./wealthfolio.js";
import type { OptionPosition, EquityPosition, Holdings } from "./snaptrade.js";

const CCY = "USD"; // Schwab 账户基础币种；如需多币种，从 balances 推导

export function optionMultiplier(p: OptionPosition): number {
  return p.symbol.option_symbol.is_mini_option ? 10 : 100;
}

export function buildOptionAssetSpec(p: OptionPosition): NewAssetInput {
  const o = p.symbol.option_symbol;
  const occ = o.ticker;
  return {
    kind: "INVESTMENT",
    quoteMode: "MANUAL",
    quoteCcy: CCY,
    instrumentType: "OPTION",
    instrumentSymbol: occ,
    displayCode: occ,
    metadata: {
      option: {
        underlyingAssetId: o.underlying_symbol.symbol,
        expiration: o.expiration_date,
        right: o.option_type,
        strike: o.strike_price,
        multiplier: String(optionMultiplier(p)),
        occSymbol: occ,
      },
    },
  };
}

export function optionPositionToHoldingsPosition(
  p: OptionPosition, assetId?: string,
): HoldingsPositionInput {
  return {
    assetId,
    symbol: p.symbol.option_symbol.ticker,
    quantity: String(p.units),
    avgCost: p.average_purchase_price == null ? undefined : String(p.average_purchase_price),
    currency: CCY,
    instrumentType: "OPTION",
  };
}

export function equityPositionToHoldingsPosition(p: EquityPosition): HoldingsPositionInput {
  return {
    symbol: p.symbol.symbol,
    quantity: String(p.units),
    avgCost: p.average_purchase_price == null ? undefined : String(p.average_purchase_price),
    currency: CCY,
    instrumentType: "EQUITY",
    exchangeMic: p.symbol.exchange?.mic_code ?? undefined,
  };
}

export function balancesToCashBalances(balances: Holdings["balances"]): Record<string, string> {
  const out: Record<string, string> = {};
  for (const b of balances) out[b.currency.code] = String(b.cash);
  return out;
}
```
> 注：若 `wealthfolio.ts` 导出的 `NewAssetInput`/`OptionSpec` 字段名与此处不一致（如 `multiplier` 类型），以 `wealthfolio.ts` 实际定义为准，按编译错误对齐（agent 已确认形状：metadata.option = {underlyingAssetId, expiration, right, strike, multiplier:string, occSymbol}）。

- [ ] **Step 4: 运行确认通过**

Run: `cd tools/snaptrade-sync && npx vitest run test/mapping.test.ts`
Expected: PASS（全部用例）。

- [ ] **Step 5: typecheck**

Run: `cd tools/snaptrade-sync && npx tsc --noEmit`
Expected: PASS（mapping 类型与 wealthfolio.ts 对齐）。

- [ ] **Step 6: Commit**

```bash
git add tools/snaptrade-sync/src/mapping.ts tools/snaptrade-sync/test/mapping.test.ts
git commit -m "feat(snaptrade-sync): pure mappers position/balance -> wealthfolio inputs"
```

---

### Task 3: state.ts — 复制 + 适配 observed 构造

**Files:**
- Create: `tools/snaptrade-sync/src/state.ts` (copy + adapt)
- Modify: `tools/snaptrade-sync/src/mapping.ts` (加 `snaptradePositionToObserved`)
- Create: `tools/snaptrade-sync/test/state.test.ts` (copy 关键用例)

- [ ] **Step 1: 复制 state.ts 并改 version**

```bash
cp ../ibkr-sync/src/state.ts ./src/state.ts
```
打开 `src/state.ts`，把 `version: 3` 改为 `version: 1`（snaptrade-sync 独立状态文件），移除/保留 `RealizedLedger` 均可（Phase 1 不喂 realized，但保留容器以便 Phase 2 复用；不删）。`reconcile` / `grace window` / `loadState` / `saveState` 逻辑**不改**。

- [ ] **Step 2: 在 mapping.ts 加 observed 构造测试**

追加到 `test/mapping.test.ts`:
```ts
import { snaptradePositionToObserved } from "../src/mapping.js";

describe("snaptradePositionToObserved", () => {
  it("option -> OPTION observed with occ/expiry/signed qty", () => {
    const o = snaptradePositionToObserved(shortPut, "opt");
    expect(o).toMatchObject({
      instrumentType: "OPTION", occSymbol: "HSAI  260618P00020000",
      expiration: "2026-06-18", quantity: "-2", currency: "USD",
    });
  });
  it("equity -> EQUITY observed", () => {
    const o = snaptradePositionToObserved(eq, "eq");
    expect(o).toMatchObject({ instrumentType: "EQUITY", occSymbol: null, quantity: "100" });
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `cd tools/snaptrade-sync && npx vitest run test/mapping.test.ts`
Expected: FAIL（`snaptradePositionToObserved` 未定义）。

- [ ] **Step 4: 实现 snaptradePositionToObserved（mapping.ts 追加）**

先看 `state.ts` 里 `ObservedPosition` 的确切字段（agent 已确认：`{contractId? / key, contractDescription, occSymbol, instrumentType, expiration, quantity, avgCost, currency}`）。SnapTrade 无 contractId，用 OCC/symbol 作 key。追加：
```ts
import type { ObservedPosition } from "./state.js";

export function snaptradePositionToObserved(
  p: OptionPosition | EquityPosition, key: string,
): ObservedPosition {
  const isOpt = "option_symbol" in (p.symbol as any);
  if (isOpt) {
    const o = (p as OptionPosition).symbol.option_symbol;
    return {
      key,
      contractDescription: o.ticker,
      occSymbol: o.ticker,
      instrumentType: "OPTION",
      expiration: o.expiration_date,
      quantity: String(p.units),
      avgCost: p.average_purchase_price == null ? "0" : String(p.average_purchase_price),
      currency: CCY,
    } as ObservedPosition;
  }
  const e = p as EquityPosition;
  return {
    key,
    contractDescription: e.symbol.symbol,
    occSymbol: null,
    instrumentType: "EQUITY",
    expiration: null,
    quantity: String(p.units),
    avgCost: p.average_purchase_price == null ? "0" : String(p.average_purchase_price),
    currency: CCY,
  } as ObservedPosition;
}
```
> 若 ibkr 的 `ObservedPosition` 用 `contractId:number` 而非 `key:string` 作主键，则在 state.ts 把该字段泛化为 `key:string`（SnapTrade 无数字合约号），并同步改 `live`/`closing` 的索引方式。这是 state.ts 适配的唯一实质改动。按编译错误对齐字段名。

- [ ] **Step 5: 复制 state 关键测试并跑**

```bash
cp ../ibkr-sync/test/state.test.ts ./test/state.test.ts
```
按需把测试里构造 `ObservedPosition` 的字段对齐（key vs contractId）。
Run: `cd tools/snaptrade-sync && npx vitest run`
Expected: PASS（mapping + snaptrade + state 全绿）。

- [ ] **Step 6: Commit**

```bash
git add tools/snaptrade-sync/src/state.ts tools/snaptrade-sync/src/mapping.ts tools/snaptrade-sync/test/state.test.ts tools/snaptrade-sync/test/mapping.test.ts
git commit -m "feat(snaptrade-sync): reuse state machine + observed-position adapter"
```

---

### Task 4: sync.ts — 编排(holdings only)

**Files:**
- Create: `tools/snaptrade-sync/src/sync.ts`

- [ ] **Step 1: 实现 sync.ts**

`src/sync.ts`（参照 ibkr-sync sync.ts 步骤 1/3/5/6/9-16，去掉 realized 与 MCP 取数）:
```ts
import { makeClient, listAccounts, fetchHoldings } from "./snaptrade.js";
import {
  optionPositionToHoldingsPosition, equityPositionToHoldingsPosition,
  buildOptionAssetSpec, balancesToCashBalances, snaptradePositionToObserved,
} from "./mapping.js";
import { loadState, saveState, reconcile, emptyState } from "./state.js";
import { WealthfolioClient, type HoldingsPositionInput } from "./wealthfolio.js";

function env(name: string, fallback?: string): string {
  const v = process.env[name] ?? fallback;
  if (v == null) throw new Error(`missing env ${name}`);
  return v;
}

export async function run(): Promise<number> {
  const dryRun = process.argv.includes("--dry-run");
  const clientId = env("SNAPTRADE_CLIENT_ID");
  const consumerKey = env("SNAPTRADE_CONSUMER_KEY");
  const userId = env("SNAPTRADE_USER_ID");
  const userSecret = env("SNAPTRADE_USER_SECRET");
  const wantInstitution = env("SNAPTRADE_INSTITUTION", "Schwab");
  const accountName = env("SNAPTRADE_ACCOUNT_NAME", "Schwab");
  const wfUrl = env("WEALTHFOLIO_URL", "http://localhost:8088");
  const statePath = env("SNAPTRADE_STATE", "state/positions-state.json");
  const graceDays = Number(env("SNAPTRADE_GRACE_DAYS", "1"));

  const st = makeClient(clientId, consumerKey);
  const accounts = await listAccounts(st, userId, userSecret);
  const acct = accounts.find(a => a.institution_name?.toLowerCase().includes(wantInstitution.toLowerCase()))
    ?? accounts[0];
  if (!acct) { console.error("[snaptrade-sync] no SnapTrade account found"); return 1; }
  const providerAccountId = `SNAPTRADE-${acct.id}`;

  const holdings = await fetchHoldings(st, userId, userSecret, acct.id);
  const today = new Date().toISOString().slice(0, 10);

  // reconcile (grace window)
  const observed = [
    ...holdings.positions.map(p => snaptradePositionToObserved(p, p.symbol.symbol)),
    ...holdings.option_positions.map(p => snaptradePositionToObserved(p, p.symbol.option_symbol.ticker)),
  ];
  const prev = loadState(statePath) ?? emptyState();
  const { next, reinjections } = reconcile(prev, observed, today, graceDays);
  next.lastRunUtc = new Date().toISOString();

  // map live positions
  const equityRows = holdings.positions.map(equityPositionToHoldingsPosition);
  const optionEntries = holdings.option_positions.map(p => ({ p, row: optionPositionToHoldingsPosition(p) }));
  const cashBalances = balancesToCashBalances(holdings.balances);

  if (dryRun) {
    console.log(JSON.stringify({ providerAccountId, equityRows, optionEntries: optionEntries.map(e => e.row), cashBalances, reinjections }, null, 2));
    return 0;
  }

  const wf = new WealthfolioClient({ baseUrl: wfUrl, password: env("WEALTHFOLIO_PASSWORD") });
  const account = await wf.findOrCreateAccount({
    name: accountName, accountType: "SECURITIES", currency: "USD",
    provider: "SNAPTRADE", providerAccountId, trackingMode: "HOLDINGS", isActive: true,
  });

  // pre-create option assets, bind assetId
  for (const e of optionEntries) {
    const asset = await wf.findOrCreateAsset(buildOptionAssetSpec(e.p));
    e.row.assetId = asset.id;
  }

  const positions: HoldingsPositionInput[] = [...equityRows, ...optionEntries.map(e => e.row)];
  const result = await wf.importHoldingsSnapshot(account.id, { date: today, positions, cashBalances });

  // option quote overwrite
  const quotes = optionEntries
    .filter(e => e.row.assetId && e.p.price != null && e.p.price > 0)
    .map(e => ({ symbol: e.row.assetId!, date: today, close: e.p.price!, currency: "USD" }));
  let quotesImported = 0;
  if (quotes.length) {
    try { await wf.importQuotes(quotes, true); quotesImported = quotes.length; }
    catch (err) { console.error("[snaptrade-sync] quote import error:", (err as Error).message); }
  }

  saveState(statePath, next);
  console.log(`[snaptrade-sync] summary: date=${today} account=${account.id} equity=${equityRows.length} options=${optionEntries.length} cash=${Object.keys(cashBalances).join(",")} snapshotsImported=${result.snapshotsImported} snapshotsFailed=${result.snapshotsFailed} quotes=${quotesImported}/${quotes.length} reinjected=${reinjections.length}`);
  return result.snapshotsFailed > 0 ? 1 : 0;
}

if (import.meta.url === `file://${process.argv[1]}`) {
  run().then(code => process.exit(code)).catch(e => { console.error("[snaptrade-sync] FATAL:", e); process.exit(1); });
}
```
> 字段名（`WealthfolioClient` 构造参数、`findOrCreateAccount`/`findOrCreateAsset`/`importHoldingsSnapshot`/`importQuotes` 签名、`Account.id`、`asset.id`）以复制进来的 `wealthfolio.ts` 实际导出为准，按编译错误对齐。

- [ ] **Step 2: typecheck**

Run: `cd tools/snaptrade-sync && npx tsc --noEmit`
Expected: PASS（如有签名不符按 wealthfolio.ts 修正）。

- [ ] **Step 3: dry-run（无需联网 wealthfolio，但需 SnapTrade 取数）**

先 `source ../../docs/snaptrade-sync/secrets.local.sh`（Task 5 建立；或临时 export 4 个 SNAPTRADE_* 变量）。
Run:
```bash
cd tools/snaptrade-sync && HTTPS_PROXY=http://127.0.0.1:7892 HTTP_PROXY=http://127.0.0.1:7892 npx tsx src/sync.ts --dry-run
```
Expected: 打印 providerAccountId + equityRows + 期权行 + cashBalances，不报错。人工核对：期权行 symbol=OCC、quantity 带符号、cashBalances.USD 合理。

- [ ] **Step 4: Commit**

```bash
git add tools/snaptrade-sync/src/sync.ts
git commit -m "feat(snaptrade-sync): orchestrator (holdings snapshot, phase 1)"
```

---

### Task 5: 脚本 + 秘钥

**Files:**
- Create: `tools/snaptrade-sync/scripts/run-snaptrade.sh`
- Create: `tools/snaptrade-sync/scripts/install-cron.sh` (copy + adapt)
- Create: `docs/snaptrade-sync/secrets.local.sh` (gitignored)
- Create: `docs/snaptrade-sync/secrets.example.sh` (committed template)

- [ ] **Step 1: 秘钥模板 + 本地秘钥**

`docs/snaptrade-sync/secrets.example.sh`（提交）:
```bash
# 复制为 secrets.local.sh（gitignored）并填值
export SNAPTRADE_CLIENT_ID="PERS-XXXX"
export SNAPTRADE_CONSUMER_KEY="xxxx"      # 测试后建议在 dashboard 轮换
export SNAPTRADE_USER_ID="wf-verify"
export SNAPTRADE_USER_SECRET="xxxx"       # registerSnapTradeUser 返回, 不可找回
export WEALTHFOLIO_URL="http://localhost:8088"
export WEALTHFOLIO_PASSWORD="xxxx"
```
`docs/snaptrade-sync/secrets.local.sh`（**不提交**，填真实值；userId/userSecret 来自验证脚手架 `snaptrade-verify/user.json`）。确认 `.gitignore` 覆盖：在仓库根或 docs 下加 `docs/snaptrade-sync/secrets.local.sh`。

- [ ] **Step 2: run-snaptrade.sh**

`tools/snaptrade-sync/scripts/run-snaptrade.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TOOL_DIR="$(dirname "$SCRIPT_DIR")"
REPO_ROOT="$(cd "$TOOL_DIR/../.." && pwd)"
export HOME="${HOME:-/home/samsung}"
export PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:$PATH"
[ -s "$HOME/.nvm/nvm.sh" ] && . "$HOME/.nvm/nvm.sh" >/dev/null 2>&1 || true
# 海外 API 走手机热点代理
export HTTPS_PROXY="${HTTPS_PROXY:-http://127.0.0.1:7892}"
export HTTP_PROXY="${HTTP_PROXY:-http://127.0.0.1:7892}"
export NO_PROXY="localhost,127.0.0.1"
# 秘钥
# shellcheck source=/dev/null
source "$REPO_ROOT/docs/snaptrade-sync/secrets.local.sh"
mkdir -p "$TOOL_DIR/logs"
LOG="$TOOL_DIR/logs/sync-$(date +%Y%m%d).log"
cd "$TOOL_DIR"
echo "=== $(date -Is) start ===" >> "$LOG"
npx tsx src/sync.ts >> "$LOG" 2>&1
echo "=== $(date -Is) end rc=$? ===" >> "$LOG"
```
```bash
chmod +x tools/snaptrade-sync/scripts/run-snaptrade.sh
```

- [ ] **Step 3: install-cron.sh**

```bash
cp ../ibkr-sync/scripts/install-cron.sh ./scripts/install-cron.sh
```
编辑：把脚本里的 `run-hourly.sh` 改为 `run-snaptrade.sh`，tag `managed-by:ibkr-sync` 改为 `managed-by:snaptrade-sync`，默认 schedule 设 `0 10,12,22 * * *`。`chmod +x`。

- [ ] **Step 4: Commit**

```bash
git add tools/snaptrade-sync/scripts/ docs/snaptrade-sync/secrets.example.sh .gitignore
git commit -m "feat(snaptrade-sync): run + cron scripts, secrets template"
```

---

### Task 6: 端到端实跑验证

**Files:** 无（验证 + 修正）

- [ ] **Step 1: 实跑写入 wealthfolio**

确认 `:8088` 容器在跑。Run:
```bash
source docs/snaptrade-sync/secrets.local.sh
cd tools/snaptrade-sync && HTTPS_PROXY=http://127.0.0.1:7892 npx tsx src/sync.ts
```
Expected: 打印 `[snaptrade-sync] summary: ... equity=13 options=24 ... snapshotsImported=1 snapshotsFailed=0 ...`。

- [ ] **Step 2: 在 wealthfolio 核对**

打开 wealthfolio Holdings 页：出现名为 "Schwab" 的账户，13 股 + 24 期权 + 现金；任取一个期权确认**市值含 ×100 乘数**（如 MSFT 270115C500：1 张 × 现价 22.975 × 100 ≈ $2297.5），与 IBKR 账户并列。

- [ ] **Step 3: 幂等复跑**

再跑一次 Step 1；确认同一天不产生重复、数值稳定（snapshot 幂等于 (accountId,date,content)）。

- [ ] **Step 4: 全量测试 + typecheck 收尾**

Run: `cd tools/snaptrade-sync && npx vitest run && npx tsc --noEmit`
Expected: 全绿。

- [ ] **Step 5: Commit（如有修正）**

```bash
git add -A tools/snaptrade-sync
git commit -m "test(snaptrade-sync): phase 1 end-to-end verified against Schwab"
```

---

## 自查（spec 覆盖 / 占位符 / 类型一致性）

- **覆盖**：spec §5 数据源→Task1；§6 映射→Task2；§7 写入→Task4；§10 状态/grace→Task3；§11 脚本/秘钥→Task5；§15 验收→Task6。§8 realized 引擎属 **Phase 2**，本计划不含（已在 spec 标注分期）。
- **占位符**：无 TODO/TBD；每个 code step 给出完整代码。`wealthfolio.ts`/`state.ts` 为"复制+具体改动"，改动点已逐条写明（version、ObservedPosition 主键 key vs contractId）。
- **类型一致性**：`HoldingsPositionInput`/`NewAssetInput`/`OptionSpec`/`ObservedPosition`/`WealthfolioClient` 方法名均来自 ibkr-sync 实际导出；各 step 反复提示"以 wealthfolio.ts/state.ts 实际定义为准，按编译错误对齐"，避免签名漂移。`optionMultiplier`/`buildOptionAssetSpec`/`snaptradePositionToObserved` 在 Task2/3 定义，Task4 使用，命名一致。

## 已知后续（非本计划）
- Phase 2：`realized.ts` 均价回放引擎 + 喂 snapshot `realized[]`。
- providerAccountId 用 `SNAPTRADE-<account id>`（稳定）已定。
