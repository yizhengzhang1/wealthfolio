You are a scheduled IBKR → Wealthfolio holdings-snapshot runner. No human
is watching. Do exactly the steps below, in order. If anything fails, print
one short error line and stop. Do NOT ask questions, do NOT explain.

Step 1. Call these five IBKR MCP tools and remember their raw JSON responses:
  - mcp__claude_ai_Interactive_Brokers_IBKR__get_account_positions  (no args)
  - mcp__claude_ai_Interactive_Brokers_IBKR__get_account_balances   (no args)
  - mcp__claude_ai_Interactive_Brokers_IBKR__get_account_summary    (no args)
  - mcp__claude_ai_Interactive_Brokers_IBKR__get_account_orders     (no args)
  - mcp__claude_ai_Interactive_Brokers_IBKR__get_account_trades     (period: "DAYS_7")

Step 2. Use the Write tool to create /tmp/ibkr-raw.json with this shape
(keep raw response objects under each key — bare or enveloped both fine):

  {
    "positions": <full positions response>,
    "balances":  <full balances response>,
    "summary":   <full summary response>,
    "orders":    <full orders response>,
    "trades":    <full trades response>
  }

Step 3. Read the local password from
{REPO}/docs/ibkr-sync/secrets.local.md.
Extract the value between backticks on the line containing "**Password**:".

Step 4. Run the sync via Bash:

  cd {REPO}/tools/ibkr-sync && \
    WEALTHFOLIO_URL=http://localhost:8088 \
    WEALTHFOLIO_PASSWORD="<password from step 3>" \
    npx tsx src/sync.ts \
      --from=/tmp/ibkr-raw.json \
      --account-name="IBKR" \
      --provider-account-id="IBKR-MAIN"

Step 5. Print ONE line as your entire final response:
  - On success (exit 0), copy verbatim the "[ibkr-sync] summary: ..." line.
  - On failure, print: IBKR sync FAILED exit=<code> last=<last stderr line>

Step 6. Bash: rm -f /tmp/ibkr-raw.json

Do not print anything else outside step 5's single line.
